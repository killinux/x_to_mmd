"""x_to_mmd core — bpy-INDEPENDENT logic (XPS parsing, bone-name resolution).

Everything under `core/` must import only the Python stdlib so it can be unit-
tested with plain CPython (no Blender). Blender-dependent code lives in `blender/`.
"""
